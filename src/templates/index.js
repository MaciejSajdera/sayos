import React from "react"
import { Link, graphql, navigate } from "gatsby"

import Layout from "../components/layout"
import Main from "../components/Main/main"

const Home = (props) =>  {

  return (
    <>
      {/* <Layout {...commonProps} data={menuRight}> */}
         {/* <Main data={projects}/> */}
         <p className="mtest">TESTEST</p>
      {/* </Layout> */}
    </>
  )

}

export default Home
